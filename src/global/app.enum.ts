export enum Gender {
    M = "male",
    F = "female"
  }


export enum SecurityAdminRole {
  DA = 'database-admin',
  GW = 'gate-worker'

}

export enum VisitPurpose {
  D = 'delivery',
  S = 'service',
  F = 'friend-family'
}